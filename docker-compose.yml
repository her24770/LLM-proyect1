services:
  app:
    build: .
    ports:
      - "8501:8501"
    restart: always
    env_file:
      - .env
    volumes:
      - ./app:/app
      - ./app/config:/app/config
      - ./app/data:/app/data
